doctype html
html
  head
    meta(charset='utf-8')
    title Criar Cliente
    link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.2/css/bootstrap.min.css')

  body
    .modal-dialog
      .modal-content
        .modal-header
          h4.modal-title Novo Cliente
        .modal-body
          if error
            .alert.alert-danger(role='alert')= error
          form(role='form', method='POST', action='/clientes/create')
            .form-group
              label(for='name') Nome:
              input#name.form-control(type='text', placeholder='nome', name='name', value=formData.name)
            .form-group
              label(for='email') Email:
              input#email.form-control(type='email', placeholder='name@email.com', name='email', value=formData.email)
            .d-flex.justify-content-between
              a.btn.btn-outline-secondary(href='/clientes') Voltar
              button.btn.btn-primary(type='submit') Adicionar Cliente
              